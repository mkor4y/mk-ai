import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts without error', (WidgetTester tester) async {
    // Basic smoke test — uygulama hatasız başlatılabilir mi?
    expect(true, isTrue);
  });
}
