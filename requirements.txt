# Telegram Bot API
python-telegram-bot==20.7

# Veri analizi ve teknik analiz
pandas>=2.2.0
numpy>=1.26.0
ta==0.10.2

# TradingView veri istemcisi (eski mcomas77/tvDatafeed reposu silindi).
# PyPI'daki surdurulen fork: ayni `from tvDatafeed import TvDatafeed` API'sini saglar.
tvdatafeed-enhanced>=2.2.1
# tvDatafeed icinde `from websocket import ...` kullaniliyor (websocket-client paketi).
websocket-client>=1.7.0

# RSS feed okuma
feedparser==6.0.10

# HTTP istekleri
requests>=2.32.0

# AI entegrasyonu
openai>=1.40.0

# Çevre değişkenleri
python-dotenv==1.0.0

# Loglama ve hata yönetimi (Python built-in kütüphaneleri - yüklemeye gerek yok)
# logging, asyncio, datetime, traceback - Python'un kendi içinde var
